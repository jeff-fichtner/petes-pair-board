<div class='list'>
  <%= form_for (@pairing || pairing) do |f| %>
    <% if making %>
      <%= f.label :start_time_text, 'Start time: ' %>
      <%= f.text_field :start_time_text %><br>
    <% end %>
      <%= f.submit 'Pair', class: 'btn' %>
  <% end %>
</div>
